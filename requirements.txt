# Base image (nedbank-de-challenge/base:1.0) already includes:
#   pyspark==3.5.0
#   delta-spark==3.1.0
#   pandas==2.1.0
#   pyarrow==14.0.0
#   pyyaml==6.0.1
#   duckdb==0.10.0
#
# Add any additional packages your pipeline requires below.
# Leave empty if base packages are sufficient.
